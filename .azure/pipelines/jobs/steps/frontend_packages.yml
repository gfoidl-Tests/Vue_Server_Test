steps:
  - task: DownloadPipelineArtifact@2
    inputs:
      artifactName: node_modules
      targetPath: frontend/node_modules

  - bash: |
      cd frontend
      chmod +x node_modules/.bin/*
      chmod +x -R node_modules/puppeteer/.local-chromium
    displayName: install node modules
